using Economy;
using Towers;
using UnityEngine;

[RequireComponent(typeof(EnergyProducer),typeof(Collider))]
public class TowerGenerator : BuildingEntity
{   // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
